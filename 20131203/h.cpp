#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    while(cin >> n) {
        if(n % 3 == 0) cout << 0 << endl;
        else cout << 1 << endl;
    }
    return 0;
}

